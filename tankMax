                            myTank.setHp(50);
                            myTank.setLive(true);
                        }
                    }
                    if (tankMax <= 0 && allTanks.size() == 1) {
                        f.removeKeyListener(tw);
                        over = true;
